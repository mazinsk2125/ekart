# EKart frontend environment variables (Create React App).
# Copy to .env and adjust as needed. CRA only exposes vars prefixed REACT_APP_.
#
# Base URL of the API Gateway. The frontend talks ONLY to the gateway.
REACT_APP_API_BASE_URL=http://localhost:8080
